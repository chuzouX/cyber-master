---
# 项目级配置（.cyber.md）
# 放在项目根目录，cyber 启动时自动加载
# frontmatter 用 YAML，含特殊字符 (*, &, !) 的值须加引号

name: "我的项目"
description: "项目一句话描述"

# 安全护栏：agent 调用工具时的边界约束
rules:
  - "仅对授权目标执行扫描"
  - "不修改生产环境数据"
  - "敏感操作前需确认"

# 项目级工具路径覆盖（追加到 PATH）
tools:
  extra_path:
    - "/usr/local/bin"
    - "D:\\tools\\bin"
---

# 项目说明

正文部分会作为系统提示注入 agent 上下文。

## 项目结构

- src/ - 源码
- tests/ - 测试

## 开发约定

- 提交前跑 cargo test
- 使用 conventional commits
